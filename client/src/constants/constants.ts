// constants.ts

// This module exports arrays of option values for status and format fields
// used in the application.

/**
 * `statusOptions` is an array of strings representing the possible
 * status options for a goal.
 */
export const statusOptions = ["Open", "Upcoming", "Completed"];

/**
 * `activityStatusOptions` is an array of strings representing the possible
 * status options for an activity.
 */
export const activityStatusOptions = ["Upcoming", "In Progress", "Completed"];

/**
 * `activityFormatOptions` is an array of strings representing the possible
 * format options for an activity.
 */
export const activityFormatOptions = [
    "BCS Webinar",
    "Webinar",
    "Online Resource",
    "Online Course",
    "Practical",
    "Conference",
    "Book",
    "Audio Book",
    "E-book",
    "Existing Role",
    "University Paper"
];
